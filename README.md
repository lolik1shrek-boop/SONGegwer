# SONGegwer

Веб-приложение для хранения, просмотра и совместного использования музыкальных табулатур и аккордов.

## Технологии

- **Backend**: Flask + SQLAlchemy + Python 3.11
- **Database**: PostgreSQL
- **Frontend**: Jinja2, HTML, CSS
- **Hosting**: Vercel (готовность для развёртывания)

## Требования

- Python 3.8+
- PostgreSQL 12+
- pip

## Установка локально

### 1. Клонирование репозитория

```bash
git clone https://github.com/lolik1shrek-boop/SONGegwer.git
cd SONGegwer
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных

Убедись, что PostgreSQL запущен, и обнови учётные данные в `app.py`:

```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://user:password@localhost:5432/songegwer'
```

### 5. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Развёртывание на Vercel

### 1. Подготовка

- Убедись, что репозиторий публичен на GitHub
- Создай переменные окружения в Vercel (Settings → Environment Variables)

### 2. Переменные окружения (Vercel)

```
DATABASE_URL = postgresql://user:password@host:port/songegwer
FLASK_ENV = production
SECRET_KEY = your-secret-key-here
```

### 3. Развёртывание

```bash
# Установи Vercel CLI
npm install -g vercel

# Развёртывание
vercel
```

## Структура проекта

```
SONGegwer/
├── app.py                 # Основное Flask приложение
├── models.py              # SQLAlchemy модели
├── db.py                  # Конфигурация БД
├── requirements.txt       # Зависимости Python
├── vercel.json            # Конфигурация Vercel
├── .env.example           # Пример переменных окружения
├── templates/             # Jinja2 шаблоны
│   ├── base.html
│   ├── index.html
│   ├── tab.html
│   ├── create.html
│   ├── edit.html
│   ├── account.html
│   ├── user_profile.html
│   └── ...
└── static/                # Статические файлы
    ├── style.css
    ├── icon_account.png
    ├── uploads/           # Загруженные аватары
    └── ...
```

## Основные функции

- 📝 **Создание табулатур**: добавление и редактирование музыкальных табов
- 👤 **Профили пользователей**: публичные профили с аватарами
- 👥 **Система подписок**: Follow/Unfollow функционал
- 🖼️ **Загрузка аватаров**: поддержка загрузки изображений профиля
- 🔐 **Аутентификация**: регистрация и вход пользователей

## Лицензия

Этот проект является учебным материалом для производственного обучения.

## Контакты

GitHub: [lolik1shrek-boop](https://github.com/lolik1shrek-boop)
